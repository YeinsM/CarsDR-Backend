name: Deploy to EC2 Windows IIS

on:
  push:
    branches:
      - develop

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    # - name: Checkout code
    #   uses: actions/checkout@v3

    # - name: Setup .NET
    #   uses: actions/setup-dotnet@v3
    #   with:
    #     dotnet-version: '8.0.x'

    # - name: Publish project
    #   run: |
    #     dotnet publish -c Release -o publish

    - name: Debug Secrets
      run: |
        echo "Host: ${{ secrets.WINRM_HOST }}"
        echo "Username: ${{ secrets.WINRM_USERNAME }}"
        echo "Password: ${{ secrets.GITHUB_TOKEN }}"
        echo "Private key exist? ${{ secrets.EC2_KEY != 'No' }}"

    # - name: Copy files to EC2 (SCP)
    #   uses: appleboy/scp-action@master
    #   with:
    #     host: ${{ secrets.WINRM_HOST }}
    #     username: ${{ secrets.WINRM_USERNAME }}
    #     password: ${{ secrets.WINRM_PASSWORD }}
    #     key: ${{ secrets.GITHUB_TOKEN }}
    #     source: "publish/"
    #     target: "C:/inetpub/wwwroot/carspot"
